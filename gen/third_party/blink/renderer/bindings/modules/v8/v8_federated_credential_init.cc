// Copyright 2019 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// DO NOT EDIT: This file is auto-generated by
// //third_party/blink/renderer/bindings/scripts/generate_bindings.py
//
// Use the GN flag `blink_enable_generated_code_formatting=true` to enable
// formatting of the generated files.

#include "third_party/blink/renderer/bindings/modules/v8/v8_federated_credential_init.h"

#include "third_party/blink/renderer/bindings/core/v8/generated_code_helper.h"
#include "third_party/blink/renderer/bindings/core/v8/native_value_traits_impl.h"
#include "third_party/blink/renderer/bindings/core/v8/to_v8_traits.h"
#include "third_party/blink/renderer/platform/bindings/exception_messages.h"
#include "third_party/blink/renderer/platform/bindings/exception_state.h"
#include "third_party/blink/renderer/platform/bindings/v8_per_isolate_data.h"

namespace blink {

FederatedCredentialInit* FederatedCredentialInit::Create(v8::Isolate* isolate, v8::Local<v8::Value> v8_value, ExceptionState& exception_state)
{
    FederatedCredentialInit* dictionary = MakeGarbageCollected<FederatedCredentialInit>(isolate);
    if (!v8_value->IsObject()) {
        const char* const class_like_name = "FederatedCredentialInit";
        exception_state.ThrowTypeError(ExceptionMessages::ValueNotOfType(class_like_name));
        return nullptr;
    }
    dictionary->FillMembersFromV8Object(isolate, v8_value.As<v8::Object>(), exception_state);
    if (UNLIKELY(exception_state.HadException())) {
        return nullptr;
    }
    return dictionary;
}

FederatedCredentialInit::FederatedCredentialInit() { }

FederatedCredentialInit::FederatedCredentialInit(v8::Isolate* isolate)
    : CredentialData(isolate)
{
}

String FederatedCredentialInit::getIconURLOr(const String& fallback_value) const
{
    if (!hasIconURL()) {
        return fallback_value;
    }
    return member_icon_url_;
}

String FederatedCredentialInit::getIconURLOr(String&& fallback_value) const
{
    if (!hasIconURL()) {
        return std::move(fallback_value);
    }
    return member_icon_url_;
}

void FederatedCredentialInit::setIconURL(const String& value)
{
    member_icon_url_ = value;
    has_icon_url_ = true;
}

void FederatedCredentialInit::setIconURL(String&& value)
{
    member_icon_url_ = std::move(value);
    has_icon_url_ = true;
}

String FederatedCredentialInit::getNameOr(const String& fallback_value) const
{
    if (!hasName()) {
        return fallback_value;
    }
    return member_name_;
}

String FederatedCredentialInit::getNameOr(String&& fallback_value) const
{
    if (!hasName()) {
        return std::move(fallback_value);
    }
    return member_name_;
}

void FederatedCredentialInit::setName(const String& value)
{
    member_name_ = value;
    has_name_ = true;
}

void FederatedCredentialInit::setName(String&& value)
{
    member_name_ = std::move(value);
    has_name_ = true;
}

String FederatedCredentialInit::getProtocolOr(const String& fallback_value) const
{
    if (!hasProtocol()) {
        return fallback_value;
    }
    return member_protocol_;
}

String FederatedCredentialInit::getProtocolOr(String&& fallback_value) const
{
    if (!hasProtocol()) {
        return std::move(fallback_value);
    }
    return member_protocol_;
}

void FederatedCredentialInit::setProtocol(const String& value)
{
    member_protocol_ = value;
    has_protocol_ = true;
}

void FederatedCredentialInit::setProtocol(String&& value)
{
    member_protocol_ = std::move(value);
    has_protocol_ = true;
}

void FederatedCredentialInit::setProvider(const String& value) { member_provider_ = value; }

void FederatedCredentialInit::setProvider(String&& value) { member_provider_ = std::move(value); }

void FederatedCredentialInit::Trace(Visitor* visitor) const
{
    TraceIfNeeded<String>::Trace(visitor, member_icon_url_);
    TraceIfNeeded<String>::Trace(visitor, member_name_);
    TraceIfNeeded<String>::Trace(visitor, member_protocol_);
    TraceIfNeeded<String>::Trace(visitor, member_provider_);
    CredentialData::Trace(visitor);
}

bool FederatedCredentialInit::FillV8ObjectWithMembers(ScriptState* script_state, v8::Local<v8::Object> v8_dictionary) const
{
    if (!CredentialData::FillV8ObjectWithMembers(script_state, v8_dictionary)) {
        return false;
    }

    v8::Local<v8::Value> v8_value;
    v8::Isolate* isolate = script_state->GetIsolate();
    v8::Local<v8::Context> current_context = isolate->GetCurrentContext();
    const auto& v8_own_member_names = GetV8OwnMemberNames(isolate);
    bool was_property_created;
    if (hasIconURL()) {
        if (!ToV8Traits<IDLUSVString>::ToV8(script_state, member_icon_url_).ToLocal(&v8_value)) {
            return false;
        }
        if (!v8_dictionary->CreateDataProperty(current_context, v8_own_member_names[0].Get(isolate), v8_value).To(&was_property_created)) {
            return false;
        }
    }
    if (hasName()) {
        if (!ToV8Traits<IDLUSVString>::ToV8(script_state, member_name_).ToLocal(&v8_value)) {
            return false;
        }
        if (!v8_dictionary->CreateDataProperty(current_context, v8_own_member_names[1].Get(isolate), v8_value).To(&was_property_created)) {
            return false;
        }
    }
    if (hasProtocol()) {
        if (!ToV8Traits<IDLString>::ToV8(script_state, member_protocol_).ToLocal(&v8_value)) {
            return false;
        }
        if (!v8_dictionary->CreateDataProperty(current_context, v8_own_member_names[2].Get(isolate), v8_value).To(&was_property_created)) {
            return false;
        }
    }
    if (hasProvider()) {
        if (!ToV8Traits<IDLUSVString>::ToV8(script_state, member_provider_).ToLocal(&v8_value)) {
            return false;
        }
        if (!v8_dictionary->CreateDataProperty(current_context, v8_own_member_names[3].Get(isolate), v8_value).To(&was_property_created)) {
            return false;
        }
    }
    return true;
}

void FederatedCredentialInit::FillMembersFromV8Object(v8::Isolate* isolate, v8::Local<v8::Object> v8_dictionary, ExceptionState& exception_state)
{
    CredentialData::FillMembersFromV8Object(isolate, v8_dictionary, exception_state);
    if (UNLIKELY(exception_state.HadException())) {
        return;
    }

    const char* const class_like_name = "FederatedCredentialInit";
    ExceptionState::ContextScope exception_context_scope(ExceptionContext(ExceptionContext::Context::kDictionaryMemberGet, class_like_name, ""), exception_state);
    exception_context_scope.ChangePropertyNameAsOptimizationHack("iconURL");
    constexpr bool is_optional = false;
    v8::Local<v8::Context> current_context = isolate->GetCurrentContext();
    const auto& v8_own_member_names = GetV8OwnMemberNames(isolate);
    v8::TryCatch try_block(isolate);
    if (!bindings::GetDictionaryMemberFromV8Object<IDLUSVString, is_optional>(
            isolate, current_context, v8_dictionary, v8_own_member_names[0].Get(isolate), has_icon_url_, member_icon_url_, try_block, exception_state)) {
        return;
    }
    exception_context_scope.ChangePropertyNameAsOptimizationHack("name");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLUSVString, is_optional>(
            isolate, current_context, v8_dictionary, v8_own_member_names[1].Get(isolate), has_name_, member_name_, try_block, exception_state)) {
        return;
    }
    exception_context_scope.ChangePropertyNameAsOptimizationHack("protocol");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLString, is_optional>(
            isolate, current_context, v8_dictionary, v8_own_member_names[2].Get(isolate), has_protocol_, member_protocol_, try_block, exception_state)) {
        return;
    }
    exception_context_scope.ChangePropertyNameAsOptimizationHack("provider");
    constexpr bool is_required = true;
    bool fallback_presence_var;
    if (!bindings::GetDictionaryMemberFromV8Object<IDLUSVString, is_required>(
            isolate, current_context, v8_dictionary, v8_own_member_names[3].Get(isolate), fallback_presence_var, member_provider_, try_block, exception_state)) {
        return;
    }
}

const base::span<const v8::Eternal<v8::Name>> FederatedCredentialInit::GetV8OwnMemberNames(v8::Isolate* isolate)
{
    static const char* const kOwnMemberNames[] = {
        "iconURL",
        "name",
        "protocol",
        "provider",
    };
    return V8PerIsolateData::From(isolate)->FindOrCreateEternalNameCache(kOwnMemberNames, kOwnMemberNames);
}

} // namespace blink
