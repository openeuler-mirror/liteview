// Copyright 2019 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// DO NOT EDIT: This file is auto-generated by
// //third_party/blink/renderer/bindings/scripts/generate_bindings.py
//
// Use the GN flag `blink_enable_generated_code_formatting=true` to enable
// formatting of the generated files.

#include "third_party/blink/renderer/bindings/modules/v8/v8_password_credential_data.h"

#include "third_party/blink/renderer/bindings/core/v8/generated_code_helper.h"
#include "third_party/blink/renderer/bindings/core/v8/native_value_traits_impl.h"
#include "third_party/blink/renderer/bindings/core/v8/to_v8_traits.h"
#include "third_party/blink/renderer/platform/bindings/exception_messages.h"
#include "third_party/blink/renderer/platform/bindings/exception_state.h"
#include "third_party/blink/renderer/platform/bindings/v8_per_isolate_data.h"

namespace blink {

PasswordCredentialData* PasswordCredentialData::Create(v8::Isolate* isolate, v8::Local<v8::Value> v8_value, ExceptionState& exception_state)
{
    PasswordCredentialData* dictionary = MakeGarbageCollected<PasswordCredentialData>(isolate);
    if (!v8_value->IsObject()) {
        const char* const class_like_name = "PasswordCredentialData";
        exception_state.ThrowTypeError(ExceptionMessages::ValueNotOfType(class_like_name));
        return nullptr;
    }
    dictionary->FillMembersFromV8Object(isolate, v8_value.As<v8::Object>(), exception_state);
    if (UNLIKELY(exception_state.HadException())) {
        return nullptr;
    }
    return dictionary;
}

PasswordCredentialData::PasswordCredentialData() { }

PasswordCredentialData::PasswordCredentialData(v8::Isolate* isolate)
    : CredentialData(isolate)
{
}

String PasswordCredentialData::getIconURLOr(const String& fallback_value) const
{
    if (!hasIconURL()) {
        return fallback_value;
    }
    return member_icon_url_;
}

String PasswordCredentialData::getIconURLOr(String&& fallback_value) const
{
    if (!hasIconURL()) {
        return std::move(fallback_value);
    }
    return member_icon_url_;
}

void PasswordCredentialData::setIconURL(const String& value)
{
    member_icon_url_ = value;
    has_icon_url_ = true;
}

void PasswordCredentialData::setIconURL(String&& value)
{
    member_icon_url_ = std::move(value);
    has_icon_url_ = true;
}

String PasswordCredentialData::getNameOr(const String& fallback_value) const
{
    if (!hasName()) {
        return fallback_value;
    }
    return member_name_;
}

String PasswordCredentialData::getNameOr(String&& fallback_value) const
{
    if (!hasName()) {
        return std::move(fallback_value);
    }
    return member_name_;
}

void PasswordCredentialData::setName(const String& value)
{
    member_name_ = value;
    has_name_ = true;
}

void PasswordCredentialData::setName(String&& value)
{
    member_name_ = std::move(value);
    has_name_ = true;
}

void PasswordCredentialData::setPassword(const String& value) { member_password_ = value; }

void PasswordCredentialData::setPassword(String&& value) { member_password_ = std::move(value); }

void PasswordCredentialData::Trace(Visitor* visitor) const
{
    TraceIfNeeded<String>::Trace(visitor, member_icon_url_);
    TraceIfNeeded<String>::Trace(visitor, member_name_);
    TraceIfNeeded<String>::Trace(visitor, member_password_);
    CredentialData::Trace(visitor);
}

bool PasswordCredentialData::FillV8ObjectWithMembers(ScriptState* script_state, v8::Local<v8::Object> v8_dictionary) const
{
    if (!CredentialData::FillV8ObjectWithMembers(script_state, v8_dictionary)) {
        return false;
    }

    v8::Local<v8::Value> v8_value;
    v8::Isolate* isolate = script_state->GetIsolate();
    v8::Local<v8::Context> current_context = isolate->GetCurrentContext();
    const auto& v8_own_member_names = GetV8OwnMemberNames(isolate);
    bool was_property_created;
    if (hasIconURL()) {
        if (!ToV8Traits<IDLUSVString>::ToV8(script_state, member_icon_url_).ToLocal(&v8_value)) {
            return false;
        }
        if (!v8_dictionary->CreateDataProperty(current_context, v8_own_member_names[0].Get(isolate), v8_value).To(&was_property_created)) {
            return false;
        }
    }
    if (hasName()) {
        if (!ToV8Traits<IDLUSVString>::ToV8(script_state, member_name_).ToLocal(&v8_value)) {
            return false;
        }
        if (!v8_dictionary->CreateDataProperty(current_context, v8_own_member_names[1].Get(isolate), v8_value).To(&was_property_created)) {
            return false;
        }
    }
    if (hasPassword()) {
        if (!ToV8Traits<IDLUSVString>::ToV8(script_state, member_password_).ToLocal(&v8_value)) {
            return false;
        }
        if (!v8_dictionary->CreateDataProperty(current_context, v8_own_member_names[2].Get(isolate), v8_value).To(&was_property_created)) {
            return false;
        }
    }
    return true;
}

void PasswordCredentialData::FillMembersFromV8Object(v8::Isolate* isolate, v8::Local<v8::Object> v8_dictionary, ExceptionState& exception_state)
{
    CredentialData::FillMembersFromV8Object(isolate, v8_dictionary, exception_state);
    if (UNLIKELY(exception_state.HadException())) {
        return;
    }

    const char* const class_like_name = "PasswordCredentialData";
    ExceptionState::ContextScope exception_context_scope(ExceptionContext(ExceptionContext::Context::kDictionaryMemberGet, class_like_name, ""), exception_state);
    exception_context_scope.ChangePropertyNameAsOptimizationHack("iconURL");
    constexpr bool is_optional = false;
    v8::Local<v8::Context> current_context = isolate->GetCurrentContext();
    const auto& v8_own_member_names = GetV8OwnMemberNames(isolate);
    v8::TryCatch try_block(isolate);
    if (!bindings::GetDictionaryMemberFromV8Object<IDLUSVString, is_optional>(
            isolate, current_context, v8_dictionary, v8_own_member_names[0].Get(isolate), has_icon_url_, member_icon_url_, try_block, exception_state)) {
        return;
    }
    exception_context_scope.ChangePropertyNameAsOptimizationHack("name");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLUSVString, is_optional>(
            isolate, current_context, v8_dictionary, v8_own_member_names[1].Get(isolate), has_name_, member_name_, try_block, exception_state)) {
        return;
    }
    exception_context_scope.ChangePropertyNameAsOptimizationHack("password");
    constexpr bool is_required = true;
    bool fallback_presence_var;
    if (!bindings::GetDictionaryMemberFromV8Object<IDLUSVString, is_required>(
            isolate, current_context, v8_dictionary, v8_own_member_names[2].Get(isolate), fallback_presence_var, member_password_, try_block, exception_state)) {
        return;
    }
}

const base::span<const v8::Eternal<v8::Name>> PasswordCredentialData::GetV8OwnMemberNames(v8::Isolate* isolate)
{
    static const char* const kOwnMemberNames[] = {
        "iconURL",
        "name",
        "password",
    };
    return V8PerIsolateData::From(isolate)->FindOrCreateEternalNameCache(kOwnMemberNames, kOwnMemberNames);
}

} // namespace blink
