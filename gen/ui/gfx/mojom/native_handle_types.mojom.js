// ui/gfx/mojom/native_handle_types.mojom.js is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2014 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

'use strict';

(function() {
  var mojomId = 'ui/gfx/mojom/native_handle_types.mojom';
  if (mojo.internal.isMojomLoaded(mojomId)) {
    console.warn('The following mojom is loaded multiple times: ' + mojomId);
    return;
  }
  mojo.internal.markMojomLoaded(mojomId);
  var bindings = mojo;
  var associatedBindings = mojo;
  var codec = mojo.internal;
  var validator = mojo.internal;

  var exports = mojo.internal.exposeNamespace('gfx.mojom');
  var shared_memory$ =
      mojo.internal.exposeNamespace('mojoBase.mojom');
  if (mojo.config.autoLoadMojomDeps) {
    mojo.internal.loadMojomIfNecessary(
        'mojo/public/mojom/base/shared_memory.mojom', '../../../mojo/public/mojom/base/shared_memory.mojom.js');
  }
  var unguessable_token$ =
      mojo.internal.exposeNamespace('mojoBase.mojom');
  if (mojo.config.autoLoadMojomDeps) {
    mojo.internal.loadMojomIfNecessary(
        'mojo/public/mojom/base/unguessable_token.mojom', '../../../mojo/public/mojom/base/unguessable_token.mojom.js');
  }



  function DXGIHandleToken(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  DXGIHandleToken.prototype.initDefaults_ = function() {
    this.value = null;
  };
  DXGIHandleToken.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  DXGIHandleToken.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 16}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    // validate DXGIHandleToken.value
    err = messageValidator.validateStructPointer(offset + codec.kStructHeaderSize + 0, unguessable_token$.UnguessableToken, false);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  DXGIHandleToken.encodedSize = codec.kStructHeaderSize + 8;

  DXGIHandleToken.decode = function(decoder) {
    var packed;
    var val = new DXGIHandleToken();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    val.value =
        decoder.decodeStructPointer(unguessable_token$.UnguessableToken);
    return val;
  };

  DXGIHandleToken.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(DXGIHandleToken.encodedSize);
    encoder.writeUint32(0);
    encoder.encodeStructPointer(unguessable_token$.UnguessableToken, val.value);
  };
  function DXGIHandle(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  DXGIHandle.prototype.initDefaults_ = function() {
    this.bufferHandle = null;
    this.token = null;
    this.sharedMemoryHandle = null;
  };
  DXGIHandle.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  DXGIHandle.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 32}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    // validate DXGIHandle.bufferHandle
    err = messageValidator.validateHandle(offset + codec.kStructHeaderSize + 0, false)
    if (err !== validator.validationError.NONE)
        return err;


    // validate DXGIHandle.token
    err = messageValidator.validateStructPointer(offset + codec.kStructHeaderSize + 8, DXGIHandleToken, false);
    if (err !== validator.validationError.NONE)
        return err;


    // validate DXGIHandle.sharedMemoryHandle
    err = messageValidator.validateStructPointer(offset + codec.kStructHeaderSize + 16, shared_memory$.UnsafeSharedMemoryRegion, true);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  DXGIHandle.encodedSize = codec.kStructHeaderSize + 24;

  DXGIHandle.decode = function(decoder) {
    var packed;
    var val = new DXGIHandle();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    val.bufferHandle =
        decoder.decodeStruct(codec.Handle);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    val.token =
        decoder.decodeStructPointer(DXGIHandleToken);
    val.sharedMemoryHandle =
        decoder.decodeStructPointer(shared_memory$.UnsafeSharedMemoryRegion);
    return val;
  };

  DXGIHandle.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(DXGIHandle.encodedSize);
    encoder.writeUint32(0);
    encoder.encodeStruct(codec.Handle, val.bufferHandle);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.encodeStructPointer(DXGIHandleToken, val.token);
    encoder.encodeStructPointer(shared_memory$.UnsafeSharedMemoryRegion, val.sharedMemoryHandle);
  };

  function GpuMemoryBufferPlatformHandle(value) {
    this.initDefault_();
    this.initValue_(value);
  }


  GpuMemoryBufferPlatformHandle.Tags = {
    sharedMemoryHandle: 0,
    dxgiHandle: 1,
  };

  GpuMemoryBufferPlatformHandle.prototype.initDefault_ = function() {
    this.$data = null;
    this.$tag = undefined;
  }

  GpuMemoryBufferPlatformHandle.prototype.initValue_ = function(value) {
    if (value == undefined) {
      return;
    }

    var keys = Object.keys(value);
    if (keys.length == 0) {
      return;
    }

    if (keys.length > 1) {
      throw new TypeError("You may set only one member on a union.");
    }

    var fields = [
        "sharedMemoryHandle",
        "dxgiHandle",
    ];

    if (fields.indexOf(keys[0]) < 0) {
      throw new ReferenceError(keys[0] + " is not a GpuMemoryBufferPlatformHandle member.");

    }

    this[keys[0]] = value[keys[0]];
  }
  Object.defineProperty(GpuMemoryBufferPlatformHandle.prototype, "sharedMemoryHandle", {
    get: function() {
      if (this.$tag != GpuMemoryBufferPlatformHandle.Tags.sharedMemoryHandle) {
        throw new ReferenceError(
            "GpuMemoryBufferPlatformHandle.sharedMemoryHandle is not currently set.");
      }
      return this.$data;
    },

    set: function(value) {
      this.$tag = GpuMemoryBufferPlatformHandle.Tags.sharedMemoryHandle;
      this.$data = value;
    }
  });
  Object.defineProperty(GpuMemoryBufferPlatformHandle.prototype, "dxgiHandle", {
    get: function() {
      if (this.$tag != GpuMemoryBufferPlatformHandle.Tags.dxgiHandle) {
        throw new ReferenceError(
            "GpuMemoryBufferPlatformHandle.dxgiHandle is not currently set.");
      }
      return this.$data;
    },

    set: function(value) {
      this.$tag = GpuMemoryBufferPlatformHandle.Tags.dxgiHandle;
      this.$data = value;
    }
  });


    GpuMemoryBufferPlatformHandle.encode = function(encoder, val) {
      if (val == null) {
        encoder.writeUint64(0);
        encoder.writeUint64(0);
        return;
      }
      if (val.$tag == undefined) {
        throw new TypeError("Cannot encode unions with an unknown member set.");
      }

      encoder.writeUint32(16);
      encoder.writeUint32(val.$tag);
      switch (val.$tag) {
        case GpuMemoryBufferPlatformHandle.Tags.sharedMemoryHandle:
          encoder.encodeStructPointer(shared_memory$.UnsafeSharedMemoryRegion, val.sharedMemoryHandle);
          break;
        case GpuMemoryBufferPlatformHandle.Tags.dxgiHandle:
          encoder.encodeStructPointer(DXGIHandle, val.dxgiHandle);
          break;
      }
      encoder.align();
    };


    GpuMemoryBufferPlatformHandle.decode = function(decoder) {
      var size = decoder.readUint32();
      if (size == 0) {
        decoder.readUint32();
        decoder.readUint64();
        return null;
      }

      var result = new GpuMemoryBufferPlatformHandle();
      var tag = decoder.readUint32();
      switch (tag) {
        case GpuMemoryBufferPlatformHandle.Tags.sharedMemoryHandle:
          result.sharedMemoryHandle = decoder.decodeStructPointer(shared_memory$.UnsafeSharedMemoryRegion);
          break;
        case GpuMemoryBufferPlatformHandle.Tags.dxgiHandle:
          result.dxgiHandle = decoder.decodeStructPointer(DXGIHandle);
          break;
      }
      decoder.align();

      return result;
    };


    GpuMemoryBufferPlatformHandle.validate = function(messageValidator, offset) {
      var size = messageValidator.decodeUnionSize(offset);
      if (size != 16) {
        return validator.validationError.INVALID_UNION_SIZE;
      }

      var tag = messageValidator.decodeUnionTag(offset);
      var data_offset = offset + 8;
      var err;
      switch (tag) {
        case GpuMemoryBufferPlatformHandle.Tags.sharedMemoryHandle:
          

    // validate GpuMemoryBufferPlatformHandle.sharedMemoryHandle
    err = messageValidator.validateStructPointer(data_offset, shared_memory$.UnsafeSharedMemoryRegion, false);
    if (err !== validator.validationError.NONE)
        return err;
          break;
        case GpuMemoryBufferPlatformHandle.Tags.dxgiHandle:
          

    // validate GpuMemoryBufferPlatformHandle.dxgiHandle
    err = messageValidator.validateStructPointer(data_offset, DXGIHandle, false);
    if (err !== validator.validationError.NONE)
        return err;
          break;
      }

      return validator.validationError.NONE;
    };

  GpuMemoryBufferPlatformHandle.encodedSize = 16;
  exports.DXGIHandleToken = DXGIHandleToken;
  exports.DXGIHandle = DXGIHandle;
  exports.GpuMemoryBufferPlatformHandle = GpuMemoryBufferPlatformHandle;
})();
